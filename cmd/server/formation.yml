---
- name: Render storage-service's api
  include_tasks: "{{ playbook_dir }}/tasks/render-service.yml"
  vars:
    service_name: api
    cmd_dir: cmd/server
    env_dest: backend/api/.env
    resources:
      secrets: [storage_clients]
      logs:    [storage_event_log]
      storage: [filesystem]
